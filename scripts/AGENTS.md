# Scripts Area

This folder contains start/stop scripts for local Docker lifecycle.

## Scripts
- macOS/Linux: `start.sh`, `stop.sh`
- Windows PowerShell: `start.ps1`, `stop.ps1`
- Windows cmd: `start.bat`, `stop.bat`

All scripts use Docker Compose project name `pm-mvp`.
