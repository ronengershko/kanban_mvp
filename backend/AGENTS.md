# Backend Area

This directory contains the FastAPI backend for the Project Management MVP.

## Current scope (Step 2)
- Minimal scaffold only
- `GET /` returns static hello world HTML
- `GET /api/health` returns `{ "status": "ok", "service": "backend" }`

## Next scope
Future steps will add static frontend serving, auth, DB persistence, and AI routes.
